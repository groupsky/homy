name: Validate Base Images Configuration

on:
  push:
    paths:
      - 'base-images/**'
      - '.github/dependabot.yml'
      - '.github/workflows/base-images.yml'
      - '.github/workflows/validate-base-images.yml'
  pull_request:
    paths:
      - 'base-images/**'
      - '.github/dependabot.yml'
      - '.github/workflows/base-images.yml'
      - '.github/workflows/validate-base-images.yml'

jobs:
  validate-configuration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Validate base images are properly configured
        run: |
          echo "🔍 Validating base images configuration..."
          echo ""

          VALIDATION_FAILED=0

          # Get list of base image directories
          BASE_IMAGE_DIRS=($(find base-images -maxdepth 1 -mindepth 1 -type d | sort))

          if [ ${#BASE_IMAGE_DIRS[@]} -eq 0 ]; then
            echo "❌ No base image directories found in base-images/"
            exit 1
          fi

          echo "Found ${#BASE_IMAGE_DIRS[@]} base image directories:"
          for dir in "${BASE_IMAGE_DIRS[@]}"; do
            echo "  • $dir"
          done
          echo ""

          # 1. Check each base image has a Dockerfile
          echo "📋 Checking Dockerfiles..."
          for dir in "${BASE_IMAGE_DIRS[@]}"; do
            if [ ! -f "$dir/Dockerfile" ]; then
              echo "  ❌ Missing Dockerfile in $dir"
              VALIDATION_FAILED=1
            else
              echo "  ✅ $dir/Dockerfile exists"
            fi
          done
          echo ""

          # 2. Check Dependabot configuration
          echo "📋 Checking Dependabot configuration..."
          for dir in "${BASE_IMAGE_DIRS[@]}"; do
            DIR_PATH="/$dir"
            if ! grep -q "directory: \"$DIR_PATH\"" .github/dependabot.yml; then
              echo "  ❌ Missing Dependabot entry for $dir"
              echo "     Add this to .github/dependabot.yml:"
              echo "     - package-ecosystem: \"docker\""
              echo "       directory: \"$DIR_PATH\""
              VALIDATION_FAILED=1
            else
              echo "  ✅ $dir configured in Dependabot"
            fi
          done
          echo ""

          # 3. Validate Dockerfile FROM lines can be parsed
          echo "📋 Validating Dockerfile FROM lines..."
          for dir in "${BASE_IMAGE_DIRS[@]}"; do
            dockerfile="$dir/Dockerfile"
            if [ -f "$dockerfile" ]; then
              FROM_LINE=$(grep "^FROM " "$dockerfile" | head -1)
              if [ -z "$FROM_LINE" ]; then
                echo "  ❌ No FROM line in $dockerfile"
                VALIDATION_FAILED=1
              else
                VERSION=$(echo "$FROM_LINE" | awk '{print $2}' | cut -d: -f2)
                if [ -z "$VERSION" ]; then
                  echo "  ❌ Cannot extract version from $dockerfile"
                  echo "     FROM line: $FROM_LINE"
                  VALIDATION_FAILED=1
                else
                  echo "  ✅ $dir extracts version: $VERSION"
                fi
              fi
            fi
          done
          echo ""

          if [ $VALIDATION_FAILED -eq 1 ]; then
            echo "❌ Base images validation failed!"
            echo ""
            echo "Please ensure:"
            echo "  1. Each base-images/* directory has a Dockerfile"
            echo "  2. Each base image is configured in .github/dependabot.yml"
            echo "  3. Each Dockerfile has a valid FROM line with version"
            exit 1
          fi

          echo "✅ All base images are properly configured!"
