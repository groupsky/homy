"""
GHCR API client for image existence checks.

Interacts with GitHub Container Registry to:
- Check if image tags already exist
- Avoid rebuilding unchanged images
- Query image metadata
"""

# TODO: Implement GHCR client
