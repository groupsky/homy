# Test environment for CI healthcheck tests (Stage 4C in ci-unified.yml)
# Used with: docker run --env-file test.env (doesn't mount Docker secrets)
# Uses direct environment variables (not _FILE pattern)
# DO NOT use real credentials - these are test values only

MQTT_USERNAME=test_user
MQTT_PASSWORD=test_password
MQTT_DEVICE_ID=test-device-12345
MQTT_APP_ID=test-app-67890
INFLUXDB_URL=http://influxdb:8086
INFLUXDB_USERNAME=test_user
INFLUXDB_PASSWORD=test_password
INFLUXDB_DATABASE=homy
